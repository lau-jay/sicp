#lang sicp
(define (p) (p))

(define (test x y)
  (if (= x 0)
      0
      y))

;; 正则序先扩展基础的表达式组成的再求值
;; (test 0 (p))
;; (if (= 0 0) 0 (p))
;; 0
;; y不会被使用到，所以不会陷入死循环
;; 应用序是先用值替换形参，所以如果实参是表达式会先求值，最后再最后归约求值
;; (test 0 (p))
;; (test 0 ((p))
;; (test 0 (((p)))
;; (test 0 ...)
;; 死循环

